# Task Manager Pro 🧠

Task Manager Pro — это полнофункциональное backend-приложение на FastAPI для управления задачами с авторизацией, CRUD-операциями и тестами. Проект создан как учебный, но архитектура и стек соответствуют требованиям реальных вакансий.

## 🚀 Стек технологий

- **FastAPI** — быстрый и современный веб-фреймворк
- **SQLAlchemy** — ORM для работы с базой данных
- **SQLite** — локальная БД (можно заменить на PostgreSQL)
- **JWT** — авторизация через токены
- **Pydantic** — валидация данных
- **Pytest** — тестирование
- **Passlib** — хеширование паролей

## 📂 Структура проекта
task_manager_pro/
├── app/
│   ├── main.py
│   ├── models.py
│   ├── database.py
│   ├── schemas.py
│   ├── auth.py
│   ├── dependencies.py
│   └── routers/
│       ├── users.py
│       └── tasks.py
├── tests/
│   └── test_tasks.py
├── .gitignore
├── README.md

## 🔐 Авторизация

- Регистрация: `POST /register`
- Логин: `POST /login`
- Получение JWT-токена
- Защита эндпоинтов через `Depends(get_current_user)`

## 📋 CRUD для задач

- `POST /tasks` — создать задачу
- `GET /tasks` — получить список задач
- `PUT /tasks/{id}` — обновить задачу
- `DELETE /tasks/{id}` — удалить задачу

## 🧪 Тесты

Запуск тестов:
```bash
pytest -v
Покрытие:

Регистрация и логин

CRUD операций

Проверка авторизации

🛠️ Запуск проекта
# Установка зависимостей
pip install -r requirements.txt

# Создание виртуального окружения
python -m venv venv
.\venv\Scripts\activate

# Запуск сервера
uvicorn app.main:app --reload
📌 Цель проекта
Проект создан как часть подготовки к собеседованиям на позицию Backend / Automation Engineer. Он демонстрирует навыки архитектурного мышления, тестирования, безопасности и работы с API.
